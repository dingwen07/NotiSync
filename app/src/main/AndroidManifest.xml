<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <!-- Runtime permission for LAN sockets and DNS-SD when targeting Android 17 / SDK 37. -->
    <uses-permission android:name="android.permission.ACCESS_LOCAL_NETWORK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Android 16 promoted ongoing notifications (Live Updates). The sender still has to request
         promotion on each eligible notification; the system/user remains the final arbiter. -->
    <uses-permission android:name="android.permission.POST_PROMOTED_NOTIFICATIONS" />
    <!-- Lets a mirrored CallStyle notification carry a full-screen intent (the platform requires a CallStyle
         notification to be a foreground service OR have an FSI, else it throws at post). On Android 14+ this is
         a user-grantable special access; when ungranted the mirrored call is shown as a heads-up, not full screen. -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- Vibrate for a mirrored incoming call: the call channel is silent (like a real dialer's), so CallRinger
         plays the system ringtone + this vibration itself, honoring the ringer mode / vibrate-when-ringing. -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.NFC" />

    <!-- iOS notification bridge (ANCS over BLE): advertise as a peripheral, connect to the iPhone, and
         hold the link in a connectedDevice foreground service. SCAN declares neverForLocation (we don't
         derive location from BLE). All are runtime permissions on minSdk 34. -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.REQUEST_COMPANION_RUN_IN_BACKGROUND" />
    <uses-permission android:name="android.permission.REQUEST_COMPANION_USE_DATA_IN_BACKGROUND" />
    <uses-permission android:name="android.permission.REQUEST_OBSERVE_COMPANION_DEVICE_PRESENCE" />
    <!-- Lets IosCompanionService.onDeviceAppeared start the connectedDevice FGS from the background when the
         associated iPhone comes into range — without it that background start throws on API 31+. -->
    <uses-permission android:name="android.permission.REQUEST_COMPANION_START_FOREGROUND_SERVICES_FROM_BACKGROUND" />
    <!-- Restart the bridge (if the user left it on) after a reboot / app update — see ios.IosBridgeBootReceiver. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <uses-feature
        android:name="android.hardware.nfc.hce"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="false" />
    <!-- Required for CompanionDeviceManager (background auto-connect); without it getSystemService returns null. -->
    <uses-feature
        android:name="android.software.companion_device_setup"
        android:required="false" />

    <!-- Scoped package visibility instead of QUERY_ALL_PACKAGES -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>

        <!-- Common notification sources with no launcher entry.
        Queries explicitly so the receiver can still resolve their label/icon. -->
        <package android:name="android" />                          <!-- Android System (framework) -->
        <package android:name="com.android.systemui" />             <!-- System UI -->
        <package android:name="com.android.phone" />                <!-- Telephony / Phone services -->
        <package android:name="com.google.android.gms" />           <!-- Google Play services -->
        <package android:name="net.extrawdw.notifly" />             <!-- Notifly diagnostic source -->
        <package android:name="moe.shizuku.privileged.api" />       <!-- External Shizuku Manager setup -->
    </queries>

    <application
        android:name=".NotiSyncApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NotiSync">

        <activity
            android:name=".MainActivity"
            android:enableOnBackInvokedCallback="true"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.NotiSync"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="https"
                    android:host="notisync.apps.extrawdw.net"
                    android:path="/pair" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="notisync"
                    android:host="pair" />
            </intent-filter>
        </activity>

        <!-- Before Android 16, URL NDEF records are delivered as ACTION_NDEF_DISCOVERED rather than
             ACTION_VIEW. Keep this on an alias so MainActivity's ordinary app-link entry point remains
             callable by browsers. Android 17 requires NFC-dispatched activities in target-37 apps to be
             protected by DISPATCH_NFC_MESSAGE, a platform permission unavailable on older releases, so
             resource-qualified flags select exactly one of these otherwise-identical aliases. -->
        <activity-alias
            android:name=".pairing.LegacyNfcPairingActivity"
            android:enabled="@bool/enable_legacy_nfc_pairing_alias"
            android:exported="true"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />

                <category android:name="android.intent.category.DEFAULT" />

                <data
                    android:scheme="https"
                    android:host="notisync.apps.extrawdw.net"
                    android:path="/pair" />
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name=".pairing.NfcPairingActivity"
            android:enabled="@bool/enable_protected_nfc_pairing_alias"
            android:exported="true"
            android:permission="android.permission.DISPATCH_NFC_MESSAGE"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />

                <category android:name="android.intent.category.DEFAULT" />

                <data
                    android:scheme="https"
                    android:host="notisync.apps.extrawdw.net"
                    android:path="/pair" />
            </intent-filter>
        </activity-alias>

        <!-- Dedicated full-screen surface for a mirrored incoming call. It lives in a separate, hidden task,
             can wake/show over keyguard, and never dismisses keyguard (the real call is on the source device). -->
        <activity
            android:name=".notification.mirror.IncomingCallActivity"
            android:enableOnBackInvokedCallback="true"
            android:excludeFromRecents="true"
            android:exported="false"
            android:launchMode="singleTask"
            android:showWhenLocked="true"
            android:taskAffinity=""
            android:theme="@style/Theme.NotiSync.CallScreen"
            android:turnScreenOn="true" />

        <!-- Provider: captures local notifications. The system binds this automatically. -->
        <service
            android:name=".notification.capture.NotiSyncListenerService"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Wake + small-message transport (production). -->
        <service
            android:name=".fcm.NotiSyncMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Hosts the iOS Bluetooth bridge so the link to a paired iPhone survives backgrounding. -->
        <service
            android:name=".ios.IosBridgeService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- Normal-app owner of an authenticated screen session. Capture/input itself runs in a short-lived
             Shizuku UserService and is intentionally not declared as an Android service. -->
        <service
            android:name=".screen.ScreenMirrorForegroundService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- Shizuku Manager delivers its binder through this provider. The external Manager remains a
             separate install and this app accepts shell-backed mode only. -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <!-- Optional reliability: the OS (re)starts the bridge when the associated iPhone is in range, after
             the user creates a CompanionDeviceManager association from the iOS tab. -->
        <service
            android:name=".ios.IosCompanionService"
            android:exported="true"
            android:permission="android.permission.BIND_COMPANION_DEVICE_SERVICE">
            <intent-filter>
                <action android:name="android.companion.CompanionDeviceService" />
            </intent-filter>
        </service>

        <!-- Resumes the iOS bridge (if the switch is on) after a reboot or an app update. Both actions are
             protected system broadcasts, so exported="true" is safe. -->
        <receiver
            android:name=".ios.IosBridgeBootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="firebase_messaging_installation_id_enabled"
            android:value="true" />

        <!-- Pairing page only: dynamically swaps the static dummy AID for the NFC Forum NDEF AID. -->
        <service
            android:name=".pairing.PairingHostApduService"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NFC_SERVICE">
            <intent-filter>
                <action android:name="android.nfc.cardemulation.action.HOST_APDU_SERVICE" />
            </intent-filter>
            <meta-data
                android:name="android.nfc.cardemulation.host_apdu_service"
                android:resource="@xml/pairing_apdu_service" />
        </service>

        <!-- Fires when the user swipes away a mirrored notification, to sync the dismissal. -->
        <receiver
            android:name=".notification.mirror.DismissReceiver"
            android:exported="false" />

        <!-- Fires when the user presses a mirrored action button (incl. inline reply), to perform it on the origin. -->
        <receiver
            android:name=".notification.mirror.MirrorActionReceiver"
            android:exported="false" />

        <!-- Run shade controls are DATA_SYNC/RUN actions, separate from notification mirroring. -->
        <receiver
            android:name=".run.RunActionReceiver"
            android:exported="false" />

        <!-- MediaRoute2 provider that publishes the mirrored media source ("Dingwen's iPhone") as this app's
             active output, so a mirrored media card's Output Switcher chip names the source device instead of
             claiming playback on this phone. Bound by the system (exported is the platform contract for route
             providers); the route itself is feature-gated and visibility-restricted to this package, and
             SELF_SCAN_ONLY (honored on newer Android) spares it other apps' scans. -->
        <service
            android:name=".notification.mirror.MirrorRouteProviderService"
            android:exported="true"
            tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="android.media.MediaRoute2ProviderService" />
                <category android:name="android.media.MediaRoute2ProviderService.SELF_SCAN_ONLY" />
            </intent-filter>
        </service>

        <!-- No-UI trampoline for tapping a mirrored notification: collapses the shade, toasts
             "Check on <device>", and asks the origin to open the real notification. -->
        <activity
            android:name=".notification.mirror.MirrorTapActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:noHistory="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.NoDisplay" />

        <!-- Shares decrypted private-asset blobs with System UI for MessagingStyle inline media. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.assetprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/asset_file_paths" />
        </provider>

        <!-- Handles Approve / Reject actions on a pending-trust notification. -->
        <receiver
            android:name=".trust.TrustActionReceiver"
            android:exported="false" />

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notisync_mirror" />

        <!-- Prefetch the Google code scanner module so the first pairing scan is instant. -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="barcode_ui" />

        <!-- play-services-code-scanner ships its scanner activity locked to PORTRAIT, which Play's
             large-screen check flags (and Android 16 ignores on large screens regardless). Relax it
             to fullUser so it honors the device's rotation setting. -->
        <activity
            android:name="com.google.mlkit.vision.codescanner.internal.GmsBarcodeScanningDelegateActivity"
            android:screenOrientation="fullUser"
            tools:node="merge"
            tools:replace="android:screenOrientation"
            tools:ignore="DiscouragedApi" /><!-- deliberate: fullUser RELAXES the 3rd-party PORTRAIT lock to the most adaptive setting -->
    </application>

</manifest>
